[tool.poetry]
name = "cordlog"
version = "0.0.2"
description = ""
authors = ["LordCode, Dybfuo"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
aiohttp = "^3.11.18"
requests = "^2.32.3"


[tool.setuptools.packages]
find = {} 

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

